{
    "$schema": "https://openapi.vercel.sh/vercel.json",
    "buildCommand": "npx convex codegen && cd apps/web && npx next build",
    "outputDirectory": "apps/web/.next",
    "installCommand": "npm install",
    "framework": "nextjs"
}
